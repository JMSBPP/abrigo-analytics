"""Test scaffold for ``simulations/`` (Task 2.4).

Hypothesis strategies live in :mod:`simulations.tests.strategies`; per-tier
unit tests live in ``test_types.py`` (Value), ``test_modules.py`` (Callable),
and ``test_utils.py`` (IO Boundary). Phase 3.3 will EXTEND this scaffold with
property tests over the Callable transforms in :mod:`simulations.modules`.
"""
