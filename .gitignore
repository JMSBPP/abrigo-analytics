# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# Data — populated by `make data` / `make data-raw` / `make panels`
# The repo ships pointers (data/manifest.yaml) and scripts; the bytes are fetched.
data/raw/
data/processed/
data/downloads/
data/probes/
data/extracted/
data/*.duckdb
data/*.duckdb.wal
data/*.parquet
!data/README.md
!data/manifest.yaml

# Notebook artifacts (binary outputs not under version control)
notebooks/**/_nbconvert_template/**/*.aux
notebooks/**/pdf/*.pdf
notebooks/**/estimates/*.pkl
!notebooks/**/estimates/.gitkeep
!notebooks/**/figures/.gitkeep
!notebooks/**/pdf/.gitkeep

# Editor / OS
.DS_Store
*~
\#*\#
.\#*
.idea/
.vscode/
*.swp

# Env
.env
.envrc
!.env.example

# Mutation testing artifacts (mutmut)
mutants/
.mutmut-cache/
.hypothesis/
.coverage

# SAAS-COHORT-1 emission outputs (Hive-partitioned + JSON sidecar)
simulations/saas_builder/data/

# Stochastic-FX variant emission outputs (parquet ensembles + JSON verdicts)
simulations/stochastic_fx/data/

# Private project memories (strategic / not for public repo)
memory/project_uhi9_hookathon_june_11.md

# LaTeX intermediates in docs/papers/ (sources + final PDFs committed; intermediates not)
docs/papers/*.aux
docs/papers/*.log
docs/papers/*.toc
docs/papers/*.out
docs/papers/*.synctex.gz
docs/papers/*.bbl
docs/papers/*.blg
docs/papers/*.fls
docs/papers/*.fdb_latexmk

